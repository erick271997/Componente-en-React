/* export function Greeting(props){
    return <h1>{props.title}</h1>
}
export function Usercard({name, amount, married,address, greet}){
    return     <div>
        <h1>{name}</h1>
        <p>{married ? "married": "single"}</p>
        <ul>
            <li>city: {address.city}</li>
            <li>direccion: {address.street}</li>
        </ul>
    </div>
}
  */